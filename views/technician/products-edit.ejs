<%- include('../partials/header') %>

<section class="py-5">
    <div class="container">
        <% if (typeof flashError !== 'undefined' && flashError && flashError.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert"><%= flashError %><button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
        <% } %>

        <div class="row g-4">
            <div class="col-lg-3">
                <%- include('../partials/techSidebar') %>
            </div>
            <div class="col-lg-9">
                <h2 class="text-white fw-bold mb-4"><i class="fa-solid fa-pen me-2 text-primary"></i>Edit Product</h2>

                <% if (product) { %>
                <div class="glass-card no-lift p-5">
                    <form method="POST" action="/technician/products/edit/<%= product.id %>" enctype="multipart/form-data">
                        <input type="hidden" name="csrf_token" value="<%= csrfToken %>">
                        <input type="hidden" name="existing_image" value="<%= product.main_image || '' %>">
                        <div class="row g-3">
                            <div class="col-md-8">
                                <label class="form-label">Product Name *</label>
                                <input type="text" name="name" class="form-control" required value="<%= product.name %>">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">SKU</label>
                                <input type="text" name="sku" class="form-control" value="<%= product.sku || '' %>">
                            </div>
                            <div class="col-12">
                                <label class="form-label">Description</label>
                                <textarea name="description" class="form-control" rows="4"><%= product.description || '' %></textarea>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Price ($) *</label>
                                <input type="number" name="price" class="form-control" required min="0" step="0.01" value="<%= product.price %>">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Discount Price ($)</label>
                                <input type="number" name="discount_price" class="form-control" min="0" step="0.01" value="<%= product.discount_price || '' %>">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Stock Quantity</label>
                                <input type="number" name="stock_quantity" class="form-control" min="0" value="<%= product.stock_quantity || 0 %>">
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Category *</label>
                                <select name="category_id" class="form-select" required>
                                    <option value="">Select category...</option>
                                    <% if (typeof categories !== 'undefined' && categories) { categories.forEach(cat => { %>
                                    <option value="<%= cat.id %>" <%= product.category_id == cat.id ? 'selected' : '' %>><%= cat.name %></option>
                                    <% }) } %>
                                </select>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">New Image (optional)</label>
                                <input type="file" name="product_image" class="form-control" accept="image/*">
                                <% if (product.main_image) { %>
                                <div class="mt-2">
                                     <%- include('../partials/productImage', { src: product.main_image ? '/uploads/products/' + product.main_image : '', alt: 'Current', style: 'height:50px;', className: 'rounded' }) %>
                                    <small class="text-muted ms-2">Current image</small>
                                </div>
                                <% } %>
                            </div>
                            <div class="col-12 text-center mt-3">
                                <button type="submit" class="btn btn-premium-primary rounded-pill px-5"><i class="fa-solid fa-save me-2"></i>Update Product</button>
                                <a href="/technician/products" class="btn btn-premium-outline rounded-pill px-4 ms-2">Cancel</a>
                            </div>
                        </div>
                    </form>
                </div>
                <% } else { %>
                <div class="empty-state">
                    <i class="fa-solid fa-exclamation-triangle fa-4x mb-3"></i>
                    <h4 class="text-white fw-bold">Product Not Found</h4>
                    <a href="/technician/products" class="btn btn-premium-primary rounded-pill">Back to Products</a>
                </div>
                <% } %>
            </div>
        </div>
    </div>
</section>

<%- include('../partials/footer') %>
