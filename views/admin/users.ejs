<%- include('../partials/header') %>

<style>
.toast-container{position:fixed;top:20px;right:20px;z-index:9999;display:flex;flex-direction:column;gap:8px}
.toast-msg{padding:10px 20px;border-radius:6px;color:#fff;font-size:14px;opacity:0;transform:translateX(30px);animation:toastIn .3s forwards}
.toast-msg.success{background:#198754}
.toast-msg.error{background:#dc3545}
@keyframes toastIn{to{opacity:1;transform:translateX(0)}}
@keyframes toastOut{to{opacity:0;transform:translateX(30px)}}
</style>
<div class="toast-container" id="toastContainer"></div>

<section class="py-5" data-aos="fade-up">
    <div class="container">
        <% if (typeof flashSuccess !== 'undefined' && flashSuccess && flashSuccess.length > 0) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert"><%= flashSuccess %><button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
        <% } %>
        <% if (typeof flashError !== 'undefined' && flashError && flashError.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert"><%= flashError %><button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
        <% } %>

        <div class="row g-4">
            <div class="col-lg-3">
                <%- include('../partials/adminSidebar') %>
            </div>
            <div class="col-lg-9">
                <div class="d-flex align-items-center gap-3 mb-4">
                    <span class="section-badge">Users</span>
                    <h2 class="text-white fw-bold mb-0 tracking-widest"><i class="fa-solid fa-users me-2 text-primary"></i>Users</h2>
                    <div class="ms-auto">
                        <a href="/admin/users/new" class="btn btn-premium-primary rounded-pill"><i class="fa-solid fa-user-plus me-2"></i>Add User</a>
                    </div>
                </div>

                <div class="glass-card p-4 mb-4">
                    <form method="GET" action="/admin/users" class="row g-2 align-items-end">
                        <div class="col-md-5">
                            <label class="form-label">Search</label>
                            <input type="text" name="search" class="form-control" placeholder="Name or email..." value="<%= searchQuery || '' %>">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Role</label>
                                <select name="role" class="form-select">
                                    <option value="">All Roles</option>
                                    <option value="customer" <%= currentRole === 'customer' ? 'selected' : '' %>>Customer</option>
                                    <option value="technician" <%= currentRole === 'technician' ? 'selected' : '' %>>Technician</option>
                                    <option value="admin" <%= currentRole === 'admin' ? 'selected' : '' %>>Admin</option>
                                    <option value="super_admin" <%= currentRole === 'super_admin' ? 'selected' : '' %>>Super Admin</option>
                                </select>
                        </div>
                        <div class="col-md-3">
                            <button type="submit" class="btn btn-premium-primary w-100 rounded-pill"><i class="fa-solid fa-search me-1"></i> Filter</button>
                        </div>
                    </form>
                </div>

                <% if (users && users.length > 0) { %>
                <div class="glass-card p-4">
                    <div class="table-responsive">
                        <table class="table table-dark align-middle mb-0 premium-table">
                            <thead>
                                <tr><th>User</th><th>Email</th><th>Role</th><th>Status</th><th>Joined</th><th>Actions</th></tr>
                            </thead>
                            <tbody>
                                <% users.forEach(u => { %>
                                <% var isSuperAdmin = u.role === 'super_admin'; var protectRow = isSuperAdmin && (!user || user.role !== 'super_admin'); %>
                                <tr>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <div class="rounded-circle bg-primary d-flex align-items-center justify-content-center me-2" style="width:36px;height:36px;flex-shrink:0;">
                                                 <% if (u.avatar) { %>
                                                     <img src="<%= imageUrl(u.avatar, 'profiles') %>" alt="" class="rounded-circle" style="width:36px;height:36px;object-fit:cover;" onerror="this.onerror=null;this.style.display='none';this.nextElementSibling.style.display='flex';">
                                                     <i class="fa-solid fa-user text-white small" style="display:none;"></i>
                                                 <% } else { %>
                                                     <i class="fa-solid fa-user text-white small"></i>
                                                 <% } %>
                                             </div>
                                            <span class="text-white fw-bold"><%= formatDisplayName(u.first_name, u.last_name) %></span>
                                            <% if (isSuperAdmin) { %><span class="badge bg-warning text-dark ms-2"><i class="fa-solid fa-shield-halved"></i></span><% } %>
                                        </div>
                                    </td>
                                    <td class="text-white-50"><%= u.email %></td>
                                    <td>
                                        <select class="form-select form-select-sm bg-dark text-white border-secondary role-select" style="width:120px;" data-user-id="<%= u.id %>" data-original="<%= u.role %>" <%= protectRow ? 'disabled' : '' %>>
                                            <option value="customer" <%= u.role === 'customer' ? 'selected' : '' %>>Customer</option>
                                            <option value="technician" <%= u.role === 'technician' ? 'selected' : '' %>>Technician</option>
                                            <option value="admin" <%= u.role === 'admin' ? 'selected' : '' %>>Admin</option>
                                            <option value="super_admin" <%= u.role === 'super_admin' ? 'selected' : '' %>>Super Admin</option>
                                        </select>
                                    </td>
                                    <td>
                                        <div class="d-flex align-items-center gap-2">
                                            <select class="form-select form-select-sm bg-dark text-white border-secondary status-select" style="width:110px;" data-user-id="<%= u.id %>" data-original="<%= u.status %>" <%= protectRow ? 'disabled' : '' %>>
                                                <option value="active" <%= u.status === 'active' ? 'selected' : '' %>>Active</option>
                                                <option value="inactive" <%= u.status === 'inactive' ? 'selected' : '' %>>Inactive</option>
                                                <option value="suspended" <%= u.status === 'suspended' ? 'selected' : '' %>>Suspended</option>
                                            </select>
                                            <% var statusColors = { active: 'bg-success', inactive: 'bg-secondary', suspended: 'bg-danger' }; %>
                                            <span class="badge text-capitalize <%= statusColors[u.status] || 'bg-secondary' %>"><%= u.status %></span>
                                        </div>
                                    </td>
                                    <td class="text-white-50"><%= u.created_at ? new Date(u.created_at).toLocaleDateString() : '' %></td>
                                    <td>
                                        <div class="d-flex gap-1">
                                            <a href="/admin/users/<%= u.id %>/edit" class="btn btn-sm btn-outline-primary rounded-pill" title="Edit"><i class="fa-solid fa-pen"></i></a>
                                            <% if (!protectRow) { %>
                                            <form method="POST" action="/admin/users/<%= u.id %>/delete" class="d-inline" data-confirm="Delete user &quot;<%= u.first_name %> <%= u.last_name %>&quot;? This cannot be undone.">
                                                <input type="hidden" name="csrf_token" value="<%= csrfToken %>">
                                                <button type="submit" class="btn btn-sm btn-outline-danger rounded-pill" title="Delete"><i class="fa-solid fa-trash"></i></button>
                                            </form>
                                            <% } %>
                                        </div>
                                    </td>
                                </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>

                <% if (pagination && pagination.totalPages > 1) { %>
                <nav class="mt-3">
                    <ul class="pagination justify-content-center">
                        <li class="page-item <%= !pagination.hasPrev ? 'disabled' : '' %>">
                            <a class="page-link bg-dark border-secondary text-white-50" href="/admin/users?page=<%= pagination.page - 1 %><%= currentRole ? '&role=' + currentRole : '' %><%= searchQuery ? '&search=' + searchQuery : '' %>">&laquo;</a>
                        </li>
                        <% for (let i = 1; i <= pagination.totalPages; i++) { %>
                        <li class="page-item <%= i === pagination.page ? 'active' : '' %>">
                            <a class="page-link <%= i === pagination.page ? 'bg-primary border-primary' : 'bg-dark border-secondary' %>" href="/admin/users?page=<%= i %><%= currentRole ? '&role=' + currentRole : '' %><%= searchQuery ? '&search=' + searchQuery : '' %>"><%= i %></a>
                        </li>
                        <% } %>
                        <li class="page-item <%= !pagination.hasNext ? 'disabled' : '' %>">
                            <a class="page-link bg-dark border-secondary text-white-50" href="/admin/users?page=<%= pagination.page + 1 %><%= currentRole ? '&role=' + currentRole : '' %><%= searchQuery ? '&search=' + searchQuery : '' %>">&raquo;</a>
                        </li>
                    </ul>
                </nav>
                <% } %>

                <% } else { %>
                <div class="empty-state">
                    <i class="fa-solid fa-users fa-4x mb-3"></i>
                    <h4 class="text-white fw-bold">No Users Found</h4>
                    <p class="text-white-50">No users match your current filters.</p>
                </div>
                <% } %>
            </div>
        </div>
    </div>
</section>

<script>
(function() {
    const CSRF = <%- JSON.stringify(csrfToken) %>;

    function showToast(msg, type) {
        var c = document.getElementById('toastContainer');
        var t = document.createElement('div');
        t.className = 'toast-msg ' + type;
        t.textContent = msg;
        c.appendChild(t);
        setTimeout(function() { t.style.animation = 'toastOut .3s forwards'; setTimeout(function() { t.remove(); }, 300); }, 2500);
    }

    function sendUpdate(url, field, value, select) {
        var orig = select.getAttribute('data-original');
        var body = {};
        body[field] = value;
        fetch(url, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json', 'X-CSRF-TOKEN': CSRF, 'Accept': 'application/json' },
            body: JSON.stringify(body)
        })
        .then(function(r) { return r.json().then(function(d) { return { ok: r.ok, data: d }; }); })
        .then(function(res) {
            if (res.ok && res.data.success) {
                select.setAttribute('data-original', value);
                showToast(res.data.message, 'success');
            } else {
                select.value = orig;
                showToast(res.data.message || 'Update failed', 'error');
            }
        })
        .catch(function() {
            select.value = orig;
            showToast('Network error', 'error');
        });
    }

    document.querySelectorAll('.role-select').forEach(function(sel) {
        sel.addEventListener('change', function() {
            sendUpdate('/admin/users/' + this.dataset.userId + '/role', 'role', this.value, this);
        });
    });

    document.querySelectorAll('.status-select').forEach(function(sel) {
        sel.addEventListener('change', function() {
            sendUpdate('/admin/users/' + this.dataset.userId + '/status', 'status', this.value, this);
        });
    });
})();
</script>

<%- include('../partials/footer') %>
